apiVersion: v1
kind: Pod
metadata:
  name: my-nginx-pod
  labels:
    component: web
    name: my-app
spec:
  containers:
  - name: my-nginx
    image: matmalecki/my-nginx
    ports:
      - containerPort: 80